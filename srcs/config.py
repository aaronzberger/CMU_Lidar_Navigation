# The name of the experiment (must be a folder in base_dir/experiments
# containing a config.json file)
exp_name = 'loss_successive_e_c'

# Base directory for this project (to save and open files)
base_dir = '/home/aaron/ag_lidar_navigation-bev'
